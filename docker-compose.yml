#docker-compose.yml
version: "3"

services:
  mysqldb:
   image: mysql:8
   container_name: mysqldb
   restart: unless-stopped
   environment:
     MYSQL_ROOT_PASSWORD: root
   volumes :
     - /home/mysql/data:/var/lib/mysql

  achatproject: 
   image: jihenesliti/jiheneback:1.0.0
   container_name: achatproject
   restart: jiheneback
   ports: 
    - 8089:8089
   depends_on:
    - mysqldb
